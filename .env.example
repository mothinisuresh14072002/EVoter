# EVoter Frontend Environment Variables
# Copy to .env.local for development or .env.production for production

# URL of the backend API service
# In Docker Compose, use http://backend:8000
# In production behind a gateway, use the public backend URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
